# Canonical Data Model — JSON Schema (expanded)

This JSON schema defines the canonical payload with provenance included for fields where applicable.

(See canonical/canonical_data_model.json for base schema; extended fields include provenance for trial_balance_records values.)
